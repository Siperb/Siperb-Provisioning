{
  "name": "siperb-provisioning",
  "version": "1.0.0",
  "description": "This project show how to Provision a Siperb Device and load Browser Phone, SIP.js JsSIP.js or your own custom Web-based SIP client.",
  "main": "Siperb-Provisioning.js",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "start": "npm run build && echo 'Build complete. Distributable files are ready.'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.6",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "rollup": "^2.79.2",
    "rollup-plugin-terser": "^7.0.2"
  },
  "dependencies": {
    "jssip": "^3.10.1",
    "sip.js": "^0.21.2"
  }
}
